<div><%= @user.first_name %> is a member of the following libraries</div>

<ul>
  <% @libraries.each do |lib| %>
    <li><%= lib.name %></li>
  <% end %>
</ul>

<%= button_to 'Join More Libraries', libraries_path, :method => :get %><br>

<%= button_to 'Add Library', new_library_path, :method => :get %><br>

<%= button_to 'Logout', logout_path, method: :delete %>